{
	"name": "genai-orchestrator-infra",
	"version": "0.1.0",
	"bin": {
		"cai-llm-orchestrator": "bin/cai-llm-orchestrator.js"
	},
	"scripts": {
		"build": "tsc",
		"watch": "tsc -w",
		"test": "jest",
		"cdk": "cdk",
		"clean": "rimraf \"!(node_modules)/**/*.js\" && rimraf \"!(node_modules)/**/*.d.ts\"",
		"deploy": "rm -rf cdk.out/* && npm run build && cdk deploy --profile fwd_innovation_tony"
	},
	"devDependencies": {
		"@types/jest": "^29.5.12",
		"ts-node": "^10.9.1",
		"typescript": "^5.1.3"
	},
	"dependencies": {
		"aws-cdk-lib": "^2.157.0",
		"dotenv": "^16.4.5"
	}
}
